---
name: ultrathink
description: Adversarial multi-perspective design review run BEFORE complex or high-risk changes — architecture decisions, new subsystems, security-sensitive or performance-critical paths, LLM-powered features, irreversible data/schema changes. Output is saved as an ADR. Not for single-file edits, typo fixes, or trivial config changes.
---

<!-- template-version: 2026-07.33 -->

# Compatibility bridge

Canonical skill: `../../../.agents/skills/ultrathink/SKILL.md`.

Read and follow that file as the complete skill. This bridge exists only so
Claude Code discovers the skill from `.claude/skills/`; it carries no
substance of its own, and a second copy of the protocol here would drift
from the canon (Invariant 3: one fact, one home).

The per-app slots this skill ships (Phase 2 stack/domain rows, the Phase 5
project gate) are filled in the canonical file, not here.
